spring:
  h2:
    console:
      enabled: true       # H2 ?? ???
      path: /h2-console   # H2 ?? ?? ?? ??
  datasource:
    url: jdbc:h2:mem:testdb   # H2 ?????? URL (In-Memory ?? ??)
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update        # ?? ??? ??
    show-sql: true            # SQL ?? ??
